/**
 * @deprecated Import desde `services/publicDirectory` en código nuevo.
 * Re-export para no romper imports existentes.
 */
export { fetchPublicOrganizationDirectory } from '../services/publicDirectory';
