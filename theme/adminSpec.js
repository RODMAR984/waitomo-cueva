/**
 * Tokens de layout / densidad para pantallas staff (`screens/admin/`).
 * Reutiliza la escala base en `mobileSpec`; usá estos nombres para ritmo vertical homogéneo.
 */
export { MOBILE_RADII, MOBILE_SIZES, MOBILE_SPACING, MOBILE_TYPE } from './mobileSpec';

/** Separación entre bloques de contenido en paneles admin (listas, métricas). */
export const ADMIN_SECTION_GAP = 16;

/** Margen interno habitual de tarjetas / secciones en admin. */
export const ADMIN_PANEL_GUTTER = 12;

/** Altura mínima de fila en tablas densas (finanzas, listados). */
export const ADMIN_TABLE_ROW_MIN_HEIGHT = 44;
